name: CodeQL

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
    
jobs:
  analyze:
    uses: cchmc/.swe-documentation/.github/workflows/codeql.yml@main